# Setup (stub)

Ubuntu prerequisites, install steps, config file location, systemd, and first-run.

*(To be filled in during implementation and packaging phases.)*

## Planned sections

- **Prerequisites:** .NET 8 runtime (or self-contained deploy), Ubuntu version support.
- **Install:** How to install (e.g. from package or from published binaries), directory layout.
- **Config file location:** Where `appsettings.json` (or equivalent) lives and how it is discovered.
- **systemd:** Enabling and starting the Watcher (and optionally WebApp) as systemd services.
- **First-run:** Initial config, creating dirs, first login to the WebApp.
