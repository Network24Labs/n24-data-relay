# Packaging (placeholder)

Placeholder for Linux packaging (e.g. .deb for Ubuntu).

Planned content:

- Package layout: binaries, config under `/etc/n24-data-relay`, data under `/var/lib/n24-data-relay`, logs under `/var/log/n24-data-relay`.
- Dependencies: .NET 8 runtime (or self-contained publish).
- systemd units installed under `/lib/systemd/system/`.

To be implemented in a later phase.
